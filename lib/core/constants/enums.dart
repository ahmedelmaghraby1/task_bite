enum FieldType { normal, content }

enum ModalType { create, edit }

enum ValidationType { phone, name, email, password, number }

enum ViewType { grid, list }

enum AlertType { success, failed, warning, information }

enum LanguageType { normal, record }
